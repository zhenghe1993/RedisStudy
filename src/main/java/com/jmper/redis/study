
持久化操作：

 RDB 定期刷数据到硬盘中 ，（不稳妥）
✔AOF 写到日志里（实时），从日志里恢复数据 ，牺牲性能，，


高可用策略：
       主从形式     主（读写）   从（读）   从（读）
       哨兵形式     主（读写）   从（读）   从（读）  哨兵（监控，将一台从节点升级为主节点---自动切换主从）
       集群模式     多个主从   组合   集群


memcache    并行
redis   多实例   串行




前端   java层   业务层  数据层



